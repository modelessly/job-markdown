{
  "manifest_version": 3,
  "name": "Job Markdown",
  "description": "Save job postings from across the web as clean local Markdown files.",
  "version": "0.1.0",
  "permissions": ["activeTab", "scripting"],
  "action": {
    "default_title": "Save job as Markdown",
    "default_popup": "popup.html"
  }
}
